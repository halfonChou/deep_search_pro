# Deep Search Pro 项目搭建与运行指南

> 按照项目的实际开发顺序，从零开始搭建并运行。

---

## 步骤1：熟悉项目整体结构

项目目录结构如下：

```
deep_search_pro/
├── .env                    # 环境变量（API密钥、数据库配置等）
├── requirements.txt        # Python依赖
├── prompt/
│   └── prompts.yml         # 主Agent和子Agent的提示词配置
├── api/
│   ├── server.py           # FastAPI服务入口（HTTP + WebSocket）
│   ├── context.py          # ContextVar会话隔离
│   └── monitor.py          # 工具调用监控 + WebSocket推送
├── agent/
│   ├── llm.py              # 模型初始化（通义千问）
│   ├── prompts.py          # 加载prompts.yml
│   ├── main_agent.py       # 主Agent编排 + 执行逻辑
│   └── subagents/
│       ├── network_search_agent.py   # 网络搜索子Agent
│       ├── database_query_agent.py   # 数据库查询子Agent
│       └── knowledge_base_agent.py   # RAGFlow知识库子Agent
├── tools/
│   ├── tavily_tool.py      # 网络搜索工具（Tavily）
│   ├── db_tools.py         # 数据库工具（MySQL）
│   ├── ragflow_tools.py    # RAGFlow知识库工具
│   ├── markdown_tools.py   # Markdown文件生成
│   ├── pdf_tools.py        # MD转PDF
│   └── upload_file_read_tool.py  # 上传文件读取
├── utils/
│   ├── path_utils.py       # 路径解析工具
│   └── word_converter.py   # MD→HTML→Word→PDF转换
├── rawflow/
│   ├── rag_config.py       # RAGFlow环境变量加载
│   ├── chat_assistant_demo.py   # RAGFlow聊天助手demo
│   └── knowledge_demo.py        # RAGFlow知识库管理demo
├── output/                 # Agent生成文件的输出目录
└── updated/                # 用户上传文件的存储目录
```

**数据流向：**

```
前端(WebSocket连接) → POST /api/task → asyncio.create_task(run_deep_agent)
                                              ↓
                                     main_agent.astream() 流式执行
                                              ↓
                           ┌─────────────────┼─────────────────┐
                     网络搜索子Agent    数据库查询子Agent    RAGFlow子Agent
                     (tavily_tool)     (db_tools)          (ragflow_tools)
                           └─────────────────┼─────────────────┘
                                              ↓
                                    生成Markdown/PDF文件
                                              ↓
                              Monitor通过WebSocket推送结果给前端
```

---

## 步骤2：创建项目 & 导入依赖

### 2.1 创建虚拟环境

```bash
# 在项目根目录下
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate
```

### 2.2 安装依赖

```bash
pip install -r requirements.txt
```

**核心依赖说明：**

| 依赖 | 作用 |
|------|------|
| `fastapi` + `uvicorn` | Web服务框架 + ASGI服务器 |
| `langchain` + `langchain-openai` | LLM应用框架 + OpenAI兼容模型调用 |
| `langgraph` | Agent编排引擎（多Agent状态图） |
| `deepagents` | LangGraph的封装库（简化多Agent创建） |
| `tavily-python` | 网络搜索API |
| `mysql-connector-python` | MySQL数据库连接 |
| `ragflow-sdk` | RAGFlow知识库SDK |
| `python-dotenv` | .env环境变量加载 |
| `pywin32` | Windows下Word COM调用（MD转PDF用） |

### 2.3 前端UI

项目本身是纯后端，前端通过WebSocket连接。如果课程提供了前端项目，启动方式通常是：

```bash
# 前端项目目录下
npm install
npm run dev
# 访问 http://localhost:5173 或类似地址
```

前端需要做两件事：
1. **POST** `http://localhost:8000/api/task` 发起任务
2. **WebSocket** `ws://localhost:8000/ws/{thread_id}` 接收实时进度

如果没有前端，可以用以下方式测试（见步骤9）。

---

## 步骤3：导入 api/ — Context + Monitor + Server

这三个文件构成了项目的"通信基础设施"。

### 3.1 `api/context.py` — 会话隔离

```python
# 核心：用ContextVar实现协程级变量隔离
_session_dir_ctx: ContextVar[Optional[str]] = ContextVar("session_dir", default=None)
_thread_id_ctx: ContextVar[Optional[str]] = ContextVar("thread_id", default=None)
```

**为什么需要它？** FastAPI的异步请求都跑在同一个线程里。如果用全局变量，User A的session_dir会被User B覆盖。ContextVar确保每个协程（每个请求）有自己独立的变量空间。

**使用方式：**
- 在Agent执行开始时 `set_session_context(path)` 存入当前会话目录
- 在任何工具函数中 `get_session_context()` 取出，无需层层传参
- 在Agent执行结束时 `reset_session_context()` 清理

### 3.2 `api/monitor.py` — 工具监控 + 消息推送

```python
# ToolMonitor：单例模式，全局唯一
monitor = ToolMonitor()

# ConnectionManager：管理所有WebSocket连接
manager = ConnectionManager()
```

**Monitor的推送链路：**
1. 工具被调用时 → `monitor.report_tool("工具名", args)`
2. Monitor内部 → 通过 `get_thread_context()` 找到当前session的thread_id
3. 通过 `manager.send_to_thread(payload, thread_id)` → 定向推送给对应前端

**关键设计：** `_emit` 方法会判断当前协程是否和FastAPI在同一个事件循环：
- 同一个循环 → `create_task()` 直接创建任务
- 不同循环/线程 → `run_coroutine_threadsafe()` 线程安全投递

### 3.3 `api/server.py` — FastAPI服务

```python
# 三个HTTP端点 + 一个WebSocket端点
POST /api/task      # 触发Agent执行（异步，立即返回）
POST /api/upload    # 上传文件到 updated/session_{id}/
GET  /api/download  # 下载output目录下的文件
GET  /api/files     # 列出output目录下的文件

WS   /ws/{thread_id}  # WebSocket实时通信
```

**启动时做了什么：**
```python
@app.on_event("startup")
async def startup_event():
    loop = asyncio.get_running_loop()
    manager.set_loop(loop)  # 把事件循环绑定到WebSocket管理器
```

这一步很关键——确保Monitor推送消息时能找到正确的事件循环。

---

## 步骤4：导入 .env 文件

`.env` 文件包含所有外部服务的连接信息：

```env
# LLM配置（通义千问，通过OpenAI兼容接口调用）
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_API_KEY=sk-xxxxxxxx
LLM_QWEN_MAX=qwen-max

# Tavily网络搜索
TAVILY_API_KEY=tvly-dev-xxxxxxxx

# MySQL数据库
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_DATABASE=pharma_db
MYSQL_HOST=localhost
MYSQL_PORT=3306

# RAGFlow知识库
RAGFLOW_API_URL=http://43.137.12.90
RAGFLOW_API_KEY=ragflow-xxxxxxxx
```

**你需要确认的外部依赖：**

| 服务 | 是否必须 | 确认方式 |
|------|----------|----------|
| 通义千问API | ✅ 必须 | 确认OPENAI_API_KEY有效，有余额 |
| Tavily API | ✅ 网络搜索必须 | 去 tavily.com 注册，免费有额度 |
| MySQL | ✅ 数据库查询必须 | 本地安装MySQL，创建 `pharma_db` 库并导入数据 |
| RAGFlow | ✅ 知识库必须 | 确认 `http://43.137.12.90` 可访问（这是远程服务器） |
| Microsoft Word | ⚠️ PDF转换需要 | 仅Windows，需安装Office（MD转PDF用Word COM接口） |

---

## 步骤5：导入 utils/ 工具

### 5.1 `utils/path_utils.py` — 路径解析

核心函数 `resolve_path(filename, session_dir)`，解决的问题：
- LLM生成的路径可能带虚拟前缀（`/workspace/`, `/mnt/data/`）→ 清洗
- 防止路径嵌套（`session_123/session_123/file.md`）→ 去重
- 区分上传文件（`updated/`目录）和生成文件（`output/`目录）→ 正确路由

### 5.2 `utils/word_converter.py` — MD转PDF

链路：`Markdown文本 → markdown库转HTML → Word COM打开HTML → 另存为PDF`

> ⚠️ 这个工具依赖Windows + Microsoft Word，如果你在Mac/Linux上开发，这个工具会失败。后续重构可以换成 weasyprint 或 reportlab。

---

## 步骤6：准备模型工具类

### `agent/llm.py`

```python
from langchain.chat_models import init_chat_model

model = init_chat_model(
    model=os.getenv("LLM_QWEN_MAX"),  # 读取.env中的 "qwen-max"
    model_provider="openai"             # 用OpenAI兼容协议调用通义千问
)
```

**原理：** 通义千问提供了OpenAI兼容的API接口（`https://dashscope.aliyuncs.com/compatible-mode/v1`），所以可以用 `langchain-openai` 的方式调用，`model_provider="openai"` 告诉LangChain用OpenAI的请求格式。

**验证模型是否可用：**
```python
# 新建test_llm.py，在项目根目录运行
from agent.llm import model
result = model.invoke("你好，请做个自我介绍")
print(result.content)
```

---

## 步骤7：创建YAML配置 & 加载提示词

### 7.1 `prompt/prompts.yml`

这个文件定义了所有Agent的"人设"：

```yaml
main_agent:
  system_prompt: |
    你是一个空调公司的智能团队负责人，负责协调三个专家助手...
    # 定义了：团队成员、工作流程、文件生成规则、执行顺序约束

sub_agents:
  tavily:
    name: "网络搜索助手"
    description: |
      负责进行网络知识搜索的智能体助手...
    system_prompt: |
      你是一个专业的网络信息查询助手...至少检索3个角度...最多5次

  db:
    name: "数据库查询助手"
    description: |
      负责进行数据库查询的智能体助手...
    system_prompt: |
      你是一个专业的数据库查询助手...先列表→预览→执行SQL

  ragflow:
    name: "RAGFlow助手"
    description: |
      负责与RAGFlow知识库进行交互的智能体助手...
    system_prompt: |
      你是一个专业的RAGFlow知识库助手...至少提问三个不同的问题
```

### 7.2 `agent/prompts.py` — 加载YAML

```python
prompt_yaml_content = load_yaml(yaml_file_path)  # 读取yml文件
main_agent_content = prompt_yaml_content["main_agent"]    # 主Agent配置
sub_agents_content = prompt_yaml_content["sub_agents"]    # 子Agent配置
```

加载后 `main_agent_content['system_prompt']` 就是主Agent的系统提示词，`sub_agents_content['tavily']` 就是网络搜索子Agent的完整配置。

---

## 步骤8：创建智能体

### 8.1 工具层（以网络搜索为例）

```python
# tools/tavily_tool.py
tavily_client = TavilyClient(api_key=os.getenv("TAVILY_API_KEY"))

@tool
def internet_search(query, topic="general", max_results=5, ...):
    monitor.report_tool("网络搜索工具", {...})  # 埋点推送
    return tavily_client.search(query=query, ...)
```

`@tool` 装饰器把普通函数变成LangChain Tool对象，Agent就能"看到"这个工具的名字、参数描述，并决定是否调用。

### 8.2 子Agent层

```python
# agent/subagents/network_search_agent.py
network_search_agent = {
    "name": sub_agents_content['tavily']['name'],           # "网络搜索助手"
    "description": sub_agents_content['tavily']['description'],  # 描述（主Agent靠这个决定是否调度）
    "system_prompt": sub_agents_content['tavily']['system_prompt'],
    "tools": [internet_search]                               # 这个子Agent能用的工具
}
```

三个子Agent结构完全一致，都是dict：`name + description + system_prompt + tools`。

### 8.3 主Agent层

```python
# agent/main_agent.py
main_agent = create_deep_agent(
    model=model,                          # 通义千问
    system_prompt=main_agent_content['system_prompt'],  # 主Agent提示词
    tools=[generate_markdown, convert_md_to_pdf, read_file_content],  # 主Agent自己的工具
    checkpointer=InMemorySaver(),         # 内存级对话记忆
    subagents=[                           # 三个子Agent
        database_query_agent,
        network_search_agent,
        knowledge_base_agent
    ]
)
```

`create_deep_agent` 内部会把子Agent转成tool_call（名字叫`task`），主Agent通过调用`task`工具来调度子Agent。

---

## 步骤9：运行项目

### 9.1 前置准备清单

在启动前，确认以下全部就绪：

```
□ Python虚拟环境已激活，依赖已安装
□ .env文件中的API Key都有效
□ MySQL已启动，pharma_db库已创建并有数据
□ RAGFlow服务可访问（ping http://43.137.12.90）
□ （可选）Tavily API Key有效
```

### 9.2 启动后端

```bash
# 在项目根目录下（deep_search_pro/）
python -m api.server
```

或者：
```bash
uvicorn api.server:app --host 0.0.0.0 --port 8000 --reload
```

看到以下输出说明启动成功：
```
[Server] WebSocket Manager bound to loop: xxxxx
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### 9.3 启动前端（如果有）

```bash
# 前端项目目录
npm install
npm run dev
```

### 9.4 没有前端？用以下方式测试

#### 方法A：用curl测试HTTP接口

```bash
# 1. 发起一个任务
curl -X POST http://localhost:8000/api/task \
  -H "Content-Type: application/json" \
  -d '{"query": "帮我搜索空调安装的注意事项"}'

# 返回：{"status":"started","thread_id":"xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"}
```

#### 方法B：用Python脚本完整测试（HTTP + WebSocket）

在项目根目录创建 `test_run.py`：

```python
import asyncio
import json
import httpx
import websockets

async def test():
    # 1. 先建立WebSocket连接
    thread_id = "test-session-001"
    
    async with websockets.connect(f"ws://localhost:8000/ws/{thread_id}") as ws:
        print(f"✅ WebSocket已连接: {thread_id}")
        
        # 2. 发起任务（HTTP POST）
        async with httpx.AsyncClient() as client:
            resp = await client.post(
                "http://localhost:8000/api/task",
                json={"query": "帮我搜索空调安装的注意事项", "thread_id": thread_id}
            )
            print(f"✅ 任务已触发: {resp.json()}")
        
        # 3. 监听WebSocket推送的实时消息
        print("\n--- 等待Agent执行，实时接收消息 ---\n")
        try:
            while True:
                msg = await asyncio.wait_for(ws.recv(), timeout=120)
                data = json.loads(msg)
                event = data.get("event", "unknown")
                message = data.get("message", "")
                
                if event == "session_created":
                    print(f"📁 工作目录: {data['data']['path']}")
                elif event == "tool_start":
                    print(f"🔧 调用工具: {data['data']['tool_name']}")
                elif event == "assistant_call":
                    print(f"🤖 调用子Agent: {data['data']['assistant_name']}")
                elif event == "task_result":
                    print(f"\n✅ 最终结果:\n{data['data']['result'][:500]}...")
                    break
                else:
                    print(f"📨 [{event}] {message}")
                    
        except asyncio.TimeoutError:
            print("⏰ 超时，Agent可能还在执行")

if __name__ == "__main__":
    asyncio.run(test())
```

```bash
# 确保server已启动，然后运行
python test_run.py
```

#### 方法C：用浏览器开发者工具测试WebSocket

1. 打开浏览器，访问 `http://localhost:8000/docs`（FastAPI自动生成的Swagger UI）
2. 在Swagger中测试 `/api/task` 接口
3. 打开浏览器控制台，手动连接WebSocket：

```javascript
const ws = new WebSocket("ws://localhost:8000/ws/test-001");
ws.onmessage = (e) => console.log(JSON.parse(e.data));
```

### 9.5 常见启动问题

| 问题 | 原因 | 解决 |
|------|------|------|
| `ModuleNotFoundError: No module named 'deepagents'` | 依赖未安装 | `pip install deepagents==0.4.3` |
| `mysql.connector.errors.ProgrammingError` | MySQL未启动或库不存在 | 启动MySQL，创建pharma_db |
| `Connection refused` RAGFlow | RAGFlow服务器不可达 | 确认服务器IP和端口，检查防火墙 |
| `win32com` 报错 | 非Windows或未安装Office | PDF转换功能暂时跳过，不影响核心流程 |
| `LLM_QWEN_MAX` 读取为None | .env中变量名有特殊字符 | 注意 `LLM_QWEN2.5` 中的 `.` 可能导致读取问题，用 `LLM_QWEN_MAX` 即可 |

---

## 模块导入顺序（代码启动时的加载链）

理解这个顺序有助于debug：

```
server.py 启动
  └── from agent.main_agent import run_deep_agent
        ├── from agent.llm import model
        │     └── 加载.env → 初始化通义千问模型
        ├── from agent.prompts import main_agent_content, sub_agents_content
        │     └── 加载prompts.yml → 解析YAML
        ├── from agent.subagents.xxx import xxx_agent
        │     ├── 导入prompts中的子Agent配置
        │     └── 导入对应的tools（每个tool会初始化自己的客户端）
        │           ├── tavily_tool → TavilyClient初始化
        │           ├── db_tools → 读取MySQL配置
        │           └── ragflow_tools → RAGFlow客户端初始化
        └── create_deep_agent() → 组装主Agent
              └── 把子Agent字典转为LangGraph节点

  └── from api.monitor import manager
        └── ConnectionManager实例化（等待startup绑定loop）
```

> 任何一个环节的依赖（API Key、数据库、服务地址）出问题，都会在 `import` 阶段报错。所以如果启动失败，看报错信息定位到具体是哪个模块的哪个外部依赖有问题。
